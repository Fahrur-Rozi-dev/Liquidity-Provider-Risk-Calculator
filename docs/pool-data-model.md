# Normalized Pool Data
Pool data fetches use \`fetchAt\` and \`updatedAt\` trackers enforcing warnings for staleness metrics against simulated intervals, returning specific token ordering metrics mapping into standard Price formulas.
